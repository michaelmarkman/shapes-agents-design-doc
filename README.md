# Shapes Agents — product design document

Static, self-contained copy of the Shapes Agents product design document
(external version). All wireframes are embedded in `index.html`.

Source project: the `shapes-design-doc` site.
